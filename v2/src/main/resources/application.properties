# CONFIGURACIÓN POR DEFECTO (LOCAL CON DOCKER)
# Spring usará esto cuando trabajes en tu PC
spring.datasource.url=jdbc:postgresql://localhost:5432/ordeplay
spring.datasource.username=pedro
spring.datasource.password=123456
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# OAUTH2 (Dejamos los huecos listos)
# spring.security.oauth2.client.registration.google.client-id=${GOOGLE_ID}
# spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_SECRET}